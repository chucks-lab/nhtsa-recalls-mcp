{
  "name": "nhtsa-recalls-mcp",
  "version": "0.1.0",
  "description": "Remote MCP server (Cloudflare Workers) exposing read-only NHTSA vehicle recall, complaint, safety-rating and VIN-decode tools to Claude, ChatGPT and other MCP clients.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "author": "Dylan Lane (Chucks-Lab)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:e2e": "node test/e2e-client.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "@modelcontextprotocol/client": "^2.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.0",
    "wrangler": "^4.131.0"
  }
}
